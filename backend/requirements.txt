fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
pydantic==2.7.1

# PyTorch ecosystem (CPU-only for local M1; override in Docker for GPU)
torch==2.3.0
torchvision==0.18.0
einops==0.8.0

# Image encoding
Pillow==10.3.0
numpy==1.26.4

# IRIS runtime deps
hydra-core==1.3.2
omegaconf==2.3.0
gym==0.21.0
pygame==2.1.2
ale-py==0.7.4

# Autointerp feature labeling (scripts/autointerp.py) — only needed to run the
# vision-LLM labeling pass; the API/server work without it (labels show 'unlabeled').
anthropic==0.40.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
